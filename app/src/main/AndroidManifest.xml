<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carnetmusculation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controllers.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controllers.SeancesActivity" />
        <activity
            android:name=".Controllers.NewSeanceActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Controllers.ExercicesActivity" />
        <activity
            android:name=".Controllers.NewExerciceActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Controllers.NewCarnetActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Controllers.PerformanceActivity">
        </activity>
        <activity android:name=".Controllers.NewPerformanceActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>